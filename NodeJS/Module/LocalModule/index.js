const info = require("./student");

//If we require only some information
//const {getName, Age} = require("./student");

console.log(info.getName());
console.log(info.getAge());
console.log(info.cgpa);
